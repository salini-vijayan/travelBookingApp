import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'travelBookingApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
